---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ${APP}-oidc-credentials
spec:
  refreshInterval: 12h
  secretStoreRef:
    kind: ClusterSecretStore
    name: akeyless-secret-store
  target:
    name: ${APP}-oidc-credentials
    template:
      data:
        KOPIA_OIDC_CLIENT_ID: "{{ .KOPIA_OIDC_CLIENT_ID }}"
        KOPIA_OIDC_CLIENT_SECRET: "{{ .KOPIA_OIDC_CLIENT_SECRET }}"
  data:
    - secretKey: KOPIA_OIDC_CLIENT_ID
      remoteRef:
        key: "${VAULT_PREFIX}/${APP}"
        property: client-id
    - secretKey: KOPIA_OIDC_CLIENT_SECRET
      remoteRef:
        key: "${VAULT_PREFIX}/${APP}"
        property: client-secret
