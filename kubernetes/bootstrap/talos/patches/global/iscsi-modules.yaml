# iscsi-modules.yaml
apiVersion: talos.dev/v1alpha1
kind: MachineConfig
metadata:
  name: node-iscsi-config
spec:
  install:
    extraKernelModules:
      - iscsi_tcp
      - libiscsi
      - scsi_transport_iscsi
