---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: storage
resources:
 - ./externalsecret.yaml
 - ./helmrelease.yaml
configMapGenerator:
  - name: synology-csi
    files:
      - client-info.yaml=./resources/client-info.yaml
generatorOptions:
  disableNameSuffixHash: true
